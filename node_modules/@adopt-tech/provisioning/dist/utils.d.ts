export interface InstallationSnippet {
    meta: string;
    script: string;
    html: string;
}
/**
 * Returns the HTML snippet that must be injected on the tenant's website
 * for the Adopt banner to load. Paste both tags in the <head> section.
 */
export declare function getInstallationSnippet(disclaimerId: string): InstallationSnippet;
//# sourceMappingURL=utils.d.ts.map