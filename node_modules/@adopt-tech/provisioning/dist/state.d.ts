import type { ProvisioningState } from './types';
export interface StateStorage {
    save(id: string, state: ProvisioningState): Promise<void>;
    load(id: string): Promise<ProvisioningState | null>;
    list(): Promise<string[]>;
    delete(id: string): Promise<void>;
}
export declare class FileStateStorage implements StateStorage {
    private readonly dir;
    constructor(dir?: string);
    private static readonly VALID_STATE_ID;
    private filePath;
    save(id: string, state: ProvisioningState): Promise<void>;
    load(id: string): Promise<ProvisioningState | null>;
    list(): Promise<string[]>;
    delete(id: string): Promise<void>;
    createCheckpoint(id: string, state: ProvisioningState, label: string): Promise<void>;
}
export declare class PersistentProvisioningState {
    private state;
    private readonly storage;
    private readonly isDryRun;
    constructor(id: string, storage: StateStorage, options?: {
        isDryRun?: boolean;
    });
    getState(): ProvisioningState;
    persist(): Promise<void>;
    load(): Promise<boolean>;
    mergeTree(incoming: ProvisioningState['tree']): void;
    updateStatus(status: ProvisioningState['status']): void;
    updateMetadata(patch: Partial<ProvisioningState['metadata']>): void;
}
//# sourceMappingURL=state.d.ts.map