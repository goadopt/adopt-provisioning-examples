"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestLogger = void 0;
class TestLogger {
    constructor() {
        this.entries = [];
        this.logger = {
            info: (data, msg) => this.capture('info', data, msg),
            warn: (data, msg) => this.capture('warn', data, msg),
            error: (data, msg) => this.capture('error', data, msg),
            debug: (data, msg) => this.capture('debug', data, msg),
            child: () => this.logger,
        };
    }
    capture(level, data, msg) {
        const message = msg ?? (typeof data === 'string' ? data : '');
        const entryData = typeof data === 'object' && data !== null ? data : undefined;
        this.entries.push({ level, message, data: entryData });
    }
    getByLevel(level) {
        return this.entries.filter((e) => e.level === level);
    }
    clear() {
        this.entries.length = 0;
    }
}
exports.TestLogger = TestLogger;
//# sourceMappingURL=test-logger.js.map