import { StateStorage } from '../state';
import { OrganizationProvisioner } from '../provisioners/organization';
export interface RebuiltTree {
    organizations: OrganizationProvisioner[];
}
/**
 * Rebuilds a provisioner tree from persisted state.
 * Used for rollback and resume operations without needing to pass the tree explicitly.
 *
 * @example
 * ```ts
 * const storage = new FileStateStorage('./provisioning-state')
 * const { organizations } = await FreshProvisionerFactory.fromStateId('my-run', storage)
 * await executor.rollback(organizations)
 * ```
 */
export declare class FreshProvisionerFactory {
    static fromStateId(stateId: string, storage: StateStorage): Promise<RebuiltTree>;
    /** Convenience: use default FileStateStorage at ./provisioning-state */
    static fromDefaultStorage(stateId: string): Promise<RebuiltTree>;
}
//# sourceMappingURL=fresh-provisioner-factory.d.ts.map