"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FreshProvisionerFactory = void 0;
/* istanbul ignore file -- factory tested via dist/ to avoid ts-jest OOM */
const state_1 = require("../state");
const errors_1 = require("../errors");
const organization_1 = require("../provisioners/organization");
const disclaimer_1 = require("../provisioners/disclaimer");
const disclaimer_style_1 = require("../provisioners/disclaimer-style");
const disclaimer_options_1 = require("../provisioners/disclaimer-options");
const terms_document_1 = require("../provisioners/terms-document");
const privacy_document_1 = require("../provisioners/privacy-document");
const cookie_document_1 = require("../provisioners/cookie-document");
const optout_1 = require("../provisioners/optout");
const vendors_1 = require("../provisioners/vendors");
const requests_page_1 = require("../provisioners/requests-page");
const member_1 = require("../provisioners/member");
const tcf_1 = require("../provisioners/tcf");
const tag_categories_1 = require("../provisioners/tag-categories");
const scan_1 = require("../provisioners/scan");
function makeProvisioner(saved) {
    const id = saved.id;
    const data = saved.data;
    const map = {
        'organization': () => new organization_1.OrganizationProvisioner(id, data),
        'disclaimer': () => new disclaimer_1.DisclaimerProvisioner(id, data),
        'disclaimer-style': () => new disclaimer_style_1.DisclaimerStyleProvisioner(id, data),
        'disclaimer-options': () => new disclaimer_options_1.DisclaimerOptionsProvisioner(id, data),
        'optout': () => new optout_1.OptoutProvisioner(id, data),
        'vendors': () => new vendors_1.VendorsProvisioner(id, data),
        'tcf': () => new tcf_1.TcfProvisioner(id, data),
        'tag-categories': () => new tag_categories_1.TagCategoriesProvisioner(id, data),
        'scan': () => new scan_1.ScanProvisioner(id, data),
        'requests-page': () => new requests_page_1.RequestsPageProvisioner(id, data),
        'member': () => new member_1.MemberProvisioner(id, data),
        'terms-document': () => new terms_document_1.TermsDocumentProvisioner(id, data),
        'privacy-document': () => new privacy_document_1.PrivacyDocumentProvisioner(id, data),
        'cookie-document': () => new cookie_document_1.CookieDocumentProvisioner(id, data),
    };
    const factory = map[saved.type];
    return factory ? factory() : null;
}
function rebuildNode(saved) {
    const provisioner = makeProvisioner(saved);
    if (!provisioner)
        return null;
    provisioner.restoreState(saved);
    for (const childSaved of saved.children) {
        const child = rebuildNode(childSaved);
        if (child)
            provisioner.addChild(child);
    }
    return provisioner;
}
/**
 * Rebuilds a provisioner tree from persisted state.
 * Used for rollback and resume operations without needing to pass the tree explicitly.
 *
 * @example
 * ```ts
 * const storage = new FileStateStorage('./provisioning-state')
 * const { organizations } = await FreshProvisionerFactory.fromStateId('my-run', storage)
 * await executor.rollback(organizations)
 * ```
 */
class FreshProvisionerFactory {
    static async fromStateId(stateId, storage) {
        const state = await storage.load(stateId);
        if (!state) {
            throw new errors_1.StateError(`No saved state found for id "${stateId}"`, errors_1.ErrorCode.STATE_NOT_FOUND);
        }
        const organizations = [];
        for (const node of state.tree) {
            const provisioner = rebuildNode(node);
            if (provisioner instanceof organization_1.OrganizationProvisioner) {
                organizations.push(provisioner);
            }
        }
        return { organizations };
    }
    /** Convenience: use default FileStateStorage at ./provisioning-state */
    static async fromDefaultStorage(stateId) {
        return FreshProvisionerFactory.fromStateId(stateId, new state_1.FileStateStorage());
    }
}
exports.FreshProvisionerFactory = FreshProvisionerFactory;
//# sourceMappingURL=fresh-provisioner-factory.js.map