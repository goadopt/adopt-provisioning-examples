"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.IdResolutionService = void 0;
const errors_1 = require("../errors");
class IdResolutionService {
    constructor() {
        this.map = new Map();
    }
    register(syntheticId, realId) {
        this.map.set(syntheticId, realId);
    }
    resolve(syntheticId) {
        const realId = this.map.get(syntheticId);
        if (!realId) {
            throw new errors_1.DependencyError(`Cannot resolve synthetic ID "${syntheticId}" — entity has not been provisioned yet`, errors_1.ErrorCode.DEPENDENCY_NOT_FOUND);
        }
        return realId;
    }
    has(syntheticId) {
        return this.map.has(syntheticId);
    }
}
exports.IdResolutionService = IdResolutionService;
//# sourceMappingURL=id-resolution-service.js.map