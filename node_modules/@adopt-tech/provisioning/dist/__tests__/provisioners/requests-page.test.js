"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const client_1 = require("../../client");
const requests_page_1 = require("../../provisioners/requests-page");
const logging_1 = require("../../logging");
jest.mock('../../client');
const MockedClient = client_1.AdoptProvisioningClient;
const URL = 'https://alpha-graph.goadopt.io/v1/graphql';
const DISC_ID = 'disc-uuid-123';
const ORG_ID = 'org-uuid-456';
function makeContext() {
    const { logger } = new logging_1.TestLogger();
    return {
        client: new MockedClient({ hasuraUrl: URL, token: 'tok' }),
        logger,
        parentId: DISC_ID,
        dryRun: false,
    };
}
beforeEach(() => MockedClient.mockClear());
describe('RequestsPageProvisioner', () => {
    it('saves with minimal required fields, defaulting style to {}', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([
            { requestPageMasterId: 'rp-1', version: 1 },
        ]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal LGPD',
            dpoUserId: 'dpo-user',
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({
            disclaimerId: DISC_ID,
            name: 'Portal LGPD',
            style: '{}',
        }));
        expect(p.getState().status).toBe('completed');
    });
    it('passes languages when provided', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([{ requestPageMasterId: 'rp-1' }]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal',
            dpoUserId: 'dpo',
            languages: { fallback: 'pt', languages: ['pt', 'en'] },
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({
            languages: { fallback: 'pt', languages: ['pt', 'en'] },
        }));
    });
    it('passes per-language texts when provided', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([{ requestPageMasterId: 'rp-1' }]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal',
            dpoUserId: 'dpo',
            texts: [{ language: 'pt', title: 'Seus direitos', introduction: 'Gerencie seus dados.' }],
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({
            texts: [{ language: 'pt', title: 'Seus direitos', introduction: 'Gerencie seus dados.' }],
        }));
    });
    it('links document masters when documents provided', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([{ requestPageMasterId: 'rp-1' }]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal',
            dpoUserId: 'dpo',
            documents: { privacyMasterId: 'priv-1', cookiesMasterId: 'cook-1' },
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({
            documents: { privacyMasterId: 'priv-1', cookiesMasterId: 'cook-1' },
        }));
    });
    it('passes legislation requests when provided', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([{ requestPageMasterId: 'rp-1' }]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal',
            dpoUserId: 'dpo',
            requests: [{ legislation: 'LGPD', requests: ['access', 'deletion'] }],
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({
            requests: [{ legislation: 'LGPD', requests: ['access', 'deletion'] }],
        }));
    });
    it('respects custom style when provided', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn().mockResolvedValue([{ requestPageMasterId: 'rp-1' }]);
        const p = new requests_page_1.RequestsPageProvisioner('dsar', {
            organizationId: ORG_ID,
            name: 'Portal',
            dpoUserId: 'dpo',
            style: '{"color":"#00DD80"}',
        });
        await p.execute(makeContext());
        expect(MockedClient.prototype.addOrEditRequestsPage).toHaveBeenCalledWith(expect.objectContaining({ style: '{"color":"#00DD80"}' }));
    });
    it('throws when parentId is missing', async () => {
        const p = new requests_page_1.RequestsPageProvisioner('dsar', { organizationId: ORG_ID, name: 'P', dpoUserId: 'dpo' });
        await expect(p.execute({ ...makeContext(), parentId: undefined })).rejects.toThrow('requires a parent');
    });
    it('dry-run does not call API', async () => {
        MockedClient.prototype.addOrEditRequestsPage = jest.fn();
        const p = new requests_page_1.RequestsPageProvisioner('dsar', { organizationId: ORG_ID, name: 'P', dpoUserId: 'dpo' });
        await p.execute({ ...makeContext(), dryRun: true });
        expect(MockedClient.prototype.addOrEditRequestsPage).not.toHaveBeenCalled();
    });
});
//# sourceMappingURL=requests-page.test.js.map