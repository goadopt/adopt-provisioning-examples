"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const client_1 = require("../client");
describe('AdoptProvisioningClient constructor', () => {
    const url = 'https://alpha-graph.goadopt.io/v1/graphql';
    it('throws when hasuraUrl is not provided', () => {
        delete process.env['ADOPT_HASURA_URL'];
        expect(() => new client_1.AdoptProvisioningClient({ token: 'tok' })).toThrow('hasuraUrl');
    });
    it('throws when neither token nor credentials are provided', () => {
        expect(() => new client_1.AdoptProvisioningClient({ hasuraUrl: url })).toThrow();
    });
    it('constructs successfully in token mode', () => {
        expect(() => new client_1.AdoptProvisioningClient({ hasuraUrl: url, token: 'my-token' })).not.toThrow();
    });
    it('constructs successfully with environment shortcut (staging)', () => {
        expect(() => new client_1.AdoptProvisioningClient({ environment: 'staging', token: 'my-token' })).not.toThrow();
    });
    it('credentials mode requires backendUrl or environment', () => {
        expect(() => new client_1.AdoptProvisioningClient({ hasuraUrl: url, username: 'u', password: 'p' }))
            .toThrow('backendUrl');
    });
    it('credentials mode succeeds when environment is set', () => {
        expect(() => new client_1.AdoptProvisioningClient({ environment: 'staging', username: 'u', password: 'p' })).not.toThrow();
    });
});
//# sourceMappingURL=client.test.js.map