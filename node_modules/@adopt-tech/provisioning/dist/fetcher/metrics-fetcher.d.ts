import { BaseFetcher } from './base-fetcher';
import type { FetchPage, FetchOptions } from './types';
export interface DailyMetric {
    id: string;
    disclaimer_id: string;
    reference_date: string;
    unique_users: number;
    opt_ins: number;
    opt_out: number;
    reject_all: number;
    download_requests: number;
    created_at?: string;
}
export interface MetricsSummary {
    unique_users: number;
    opt_ins: number;
    opt_out: number;
    reject_all: number;
    download_requests: number;
}
export interface MetricsFilter {
    disclaimerId?: string;
    /** ISO date string — fetch metrics from this date onwards */
    fromDate?: string;
    /** ISO date string — fetch metrics up to this date */
    toDate?: string;
}
export declare class MetricsFetcher extends BaseFetcher<DailyMetric, MetricsFilter> {
    fetch(options?: FetchOptions): Promise<FetchPage<DailyMetric>>;
    /** Fetch aggregated totals for the current filter — computed client-side over all rows */
    summary(): Promise<MetricsSummary>;
    where(filter: MetricsFilter): MetricsFetcher;
    private buildMetricsWhere;
}
//# sourceMappingURL=metrics-fetcher.d.ts.map