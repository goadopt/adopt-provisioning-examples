import { AdoptProvisioningClient } from '../client';
export interface DisclaimerVersions {
    id: string;
    disclaimer_id: string;
    main_version: number;
    tags_version: number;
    disclaimer_options_version: number;
    disclaimer_style_version: number;
    disclaimer_website_version: number;
    vendors_version: number;
    opt_out_version: number;
    terms_version: number;
    policy_version: number;
    cookie_version: number;
    request_page_version: number;
    documents_version: number;
}
/**
 * Fetches the current active version numbers for all sub-entities of a disclaimer.
 * Use this before fetching tags, options, style, etc. to ensure you get the latest
 * version and not stale data from a previous scan or configuration update.
 *
 * @example
 * ```ts
 * const versions = await getDisclaimerVersions(client, disclaimerId)
 * // Now fetch tags at the correct version:
 * const tags = await createFetcher(client)
 *   .tags({ disclaimerId, version: versions.tags_version })
 *   .fetch()
 * ```
 */
export declare function getDisclaimerVersions(client: AdoptProvisioningClient, disclaimerId: string): Promise<DisclaimerVersions | null>;
//# sourceMappingURL=versions-fetcher.d.ts.map