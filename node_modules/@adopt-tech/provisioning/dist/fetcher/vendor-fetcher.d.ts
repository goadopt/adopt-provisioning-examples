import { BaseFetcher } from './base-fetcher';
import type { VendorData } from '../types';
import type { VendorFilter, FetchPage, FetchOptions } from './types';
export declare class VendorFetcher extends BaseFetcher<VendorData, VendorFilter> {
    /**
     * Fetch vendors — automatically uses the current vendors_version so you only
     * see the active vendor assignments, not leftover rows from past updates.
     */
    fetch(options?: FetchOptions): Promise<FetchPage<VendorData>>;
    where(filter: VendorFilter): VendorFetcher;
    private buildVendorWhere;
}
//# sourceMappingURL=vendor-fetcher.d.ts.map