import { AdoptProvisioningClient } from '../client';
import type { FetchPage, FetchOptions } from './types';
export declare abstract class BaseFetcher<TEntity, TFilter = Record<string, unknown>> {
    protected readonly client: AdoptProvisioningClient;
    protected filter: TFilter;
    constructor(client: AdoptProvisioningClient, filter: TFilter);
    abstract fetch(options?: FetchOptions): Promise<FetchPage<TEntity>>;
    stream(options?: FetchOptions): AsyncGenerator<TEntity>;
    protected buildWhere(filter: TFilter): Record<string, unknown>;
}
//# sourceMappingURL=base-fetcher.d.ts.map