"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OrganizationFetcher = void 0;
const base_fetcher_1 = require("./base-fetcher");
const disclaimer_fetcher_1 = require("./disclaimer-fetcher");
const member_fetcher_1 = require("./member-fetcher");
const organization_1 = require("../graphql/queries/organization");
class OrganizationFetcher extends base_fetcher_1.BaseFetcher {
    async fetch(options = {}) {
        const where = this.buildOrganizationWhere();
        const limit = options.limit ?? 50;
        const result = await this.client.query(organization_1.GET_ORGANIZATIONS, {
            where,
            limit,
            offset: options.offset ?? 0,
            order_by: options.orderBy ?? { created_at: 'desc' },
        });
        return {
            data: result.organization,
            totalCount: null, // organization_aggregate not available for user role
            hasMore: result.organization.length === limit,
        };
    }
    async getById(id) {
        const result = await this.client.query(organization_1.GET_ORGANIZATION_BY_ID, { id });
        return result.organization_by_pk;
    }
    disclaimers(filter = {}) {
        return new disclaimer_fetcher_1.DisclaimerFetcher(this.client, {
            organizationId: this.filter.id,
            ...filter,
        });
    }
    members() {
        return new member_fetcher_1.MemberFetcher(this.client, { organizationId: this.filter.id });
    }
    where(filter) {
        return new OrganizationFetcher(this.client, { ...this.filter, ...filter });
    }
    buildOrganizationWhere() {
        const where = { deleted_at: { _is_null: true } };
        if (this.filter.id)
            where['id'] = { _eq: this.filter.id };
        if (this.filter.pathname)
            where['pathname'] = { _eq: this.filter.pathname };
        if (this.filter.name)
            where['name'] = { _ilike: `%${this.filter.name}%` };
        return where;
    }
}
exports.OrganizationFetcher = OrganizationFetcher;
//# sourceMappingURL=organization-fetcher.js.map