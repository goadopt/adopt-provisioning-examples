"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DisclaimerFetcher = void 0;
const base_fetcher_1 = require("./base-fetcher");
const tags_fetcher_1 = require("./tags-fetcher");
const vendor_fetcher_1 = require("./vendor-fetcher");
const disclaimer_1 = require("../graphql/queries/disclaimer");
const disclaimer_sub_1 = require("../graphql/queries/disclaimer-sub");
const versions_fetcher_1 = require("./versions-fetcher");
async function resolveVersions(client, disclaimerId) {
    return (0, versions_fetcher_1.getDisclaimerVersions)(client, disclaimerId);
}
class DisclaimerFetcher extends base_fetcher_1.BaseFetcher {
    async fetch(options = {}) {
        const where = this.buildDisclaimerWhere();
        const result = await this.client.query(disclaimer_1.GET_DISCLAIMERS, {
            where,
            limit: options.limit ?? 50,
            offset: options.offset ?? 0,
            order_by: options.orderBy ?? { created_at: 'desc' },
        });
        const totalCount = result.disclaimers_aggregate?.aggregate?.count ?? null;
        // Post-process: filter disclaimer_websites to the current version
        const data = result.disclaimers.map((disc) => {
            const raw = disc;
            const vers = raw['disclaimer_versions'];
            const targetVersion = vers?.[0]?.disclaimer_website_version;
            if (targetVersion !== undefined && raw['disclaimer_websites']) {
                const websites = raw['disclaimer_websites'];
                raw['disclaimer_websites'] = websites.filter((w) => w.version === targetVersion);
            }
            return disc;
        });
        return {
            data,
            totalCount,
            hasMore: result.disclaimers.length === (options.limit ?? 50),
        };
    }
    async getById(id) {
        const result = await this.client.query(disclaimer_1.GET_DISCLAIMER_BY_ID, { id });
        const disc = result.disclaimers_by_pk;
        if (!disc)
            return null;
        // Post-process: filter disclaimer_websites to the current version
        const raw = disc;
        const vers = raw['disclaimer_versions'];
        const targetVersion = vers?.[0]?.disclaimer_website_version;
        if (targetVersion !== undefined && raw['disclaimer_websites']) {
            const websites = raw['disclaimer_websites'];
            raw['disclaimer_websites'] = websites.filter((w) => w.version === targetVersion);
        }
        return disc;
    }
    /** Current active version numbers for all sub-entities */
    async getVersions(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getVersions()');
        return resolveVersions(this.client, id);
    }
    /** Domains (disclaimer_websites) at the current disclaimer_website_version */
    async getDomains(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getDomains()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return [];
        const result = await this.client.query(disclaimer_sub_1.GET_DISCLAIMER_WEBSITES_AT_VERSION, { disclaimerId: id, version: versions.disclaimer_website_version });
        return result.disclaimer_website;
    }
    /** Options at the current disclaimer_options_version */
    async getOptions(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getOptions()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return null;
        const result = await this.client.query(disclaimer_1.GET_DISCLAIMER_OPTIONS_AT_VERSION, { disclaimerId: id, version: versions.disclaimer_options_version });
        return result.disclaimer_options[0] ?? null;
    }
    /** Terms document link at the current terms_version */
    async getTerms(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getTerms()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return null;
        const result = await this.client.query(disclaimer_sub_1.GET_DISCLAIMER_TERMS_AT_VERSION, { disclaimerId: id, version: versions.terms_version });
        return result.terms[0] ?? null;
    }
    /** Privacy policy link at the current policy_version */
    async getPolicy(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getPolicy()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return null;
        const result = await this.client.query(disclaimer_sub_1.GET_DISCLAIMER_POLICY_AT_VERSION, { disclaimerId: id, version: versions.policy_version });
        return result.policy[0] ?? null;
    }
    /** Optout configuration at the current opt_out_version */
    async getOptout(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getOptout()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return null;
        const result = await this.client.query(disclaimer_sub_1.GET_DISCLAIMER_OPTOUT_AT_VERSION, { disclaimerId: id, version: versions.opt_out_version });
        return result.opt_out[0] ?? null;
    }
    /** Linked document master IDs at the current documents_version */
    async getDocuments(disclaimerId) {
        const id = disclaimerId ?? this.filter.id;
        if (!id)
            throw new Error('disclaimerId is required for getDocuments()');
        const versions = await resolveVersions(this.client, id);
        if (!versions)
            return null;
        const result = await this.client.query(disclaimer_sub_1.GET_DISCLAIMER_DOCUMENTS_AT_VERSION, { disclaimerId: id, version: versions.documents_version });
        return result.disclaimer_documents[0] ?? null;
    }
    /** Chain to tags fetcher (version auto-resolved) */
    tags() {
        return new tags_fetcher_1.TagsFetcher(this.client, { disclaimerId: this.filter.id });
    }
    /** Chain to vendors fetcher (version auto-resolved) */
    vendors() {
        return new vendor_fetcher_1.VendorFetcher(this.client, { disclaimerId: this.filter.id });
    }
    where(filter) {
        return new DisclaimerFetcher(this.client, { ...this.filter, ...filter });
    }
    buildDisclaimerWhere() {
        const where = { deleted_at: { _is_null: true } };
        if (this.filter.id)
            where['id'] = { _eq: this.filter.id };
        if (this.filter.organizationId)
            where['organization_id'] = { _eq: this.filter.organizationId };
        if (this.filter.pathname)
            where['pathname'] = { _eq: this.filter.pathname };
        return where;
    }
}
exports.DisclaimerFetcher = DisclaimerFetcher;
//# sourceMappingURL=disclaimer-fetcher.js.map