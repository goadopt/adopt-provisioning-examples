"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MetricsFetcher = void 0;
const base_fetcher_1 = require("./base-fetcher");
const metrics_1 = require("../graphql/queries/metrics");
class MetricsFetcher extends base_fetcher_1.BaseFetcher {
    async fetch(options = {}) {
        const where = this.buildMetricsWhere();
        const limit = options.limit ?? 90;
        const result = await this.client.query(metrics_1.GET_DAILY_METRICS, {
            where,
            limit,
            offset: options.offset ?? 0,
            order_by: options.orderBy ?? [{ reference_date: 'desc' }],
        });
        return {
            data: result.daily_metrics,
            totalCount: null,
            hasMore: result.daily_metrics.length === limit,
        };
    }
    /** Fetch aggregated totals for the current filter — computed client-side over all rows */
    async summary() {
        const where = this.buildMetricsWhere();
        const result = await this.client.query(metrics_1.GET_DAILY_METRICS, { where, limit: 10000, offset: 0 });
        const zero = { unique_users: 0, opt_ins: 0, opt_out: 0, reject_all: 0, download_requests: 0 };
        return result.daily_metrics.reduce((acc, row) => ({
            unique_users: acc.unique_users + (row.unique_users ?? 0),
            opt_ins: acc.opt_ins + (row.opt_ins ?? 0),
            opt_out: acc.opt_out + (row.opt_out ?? 0),
            reject_all: acc.reject_all + (row.reject_all ?? 0),
            download_requests: acc.download_requests + (row.download_requests ?? 0),
        }), zero);
    }
    where(filter) {
        return new MetricsFetcher(this.client, { ...this.filter, ...filter });
    }
    buildMetricsWhere() {
        const where = {};
        if (this.filter.disclaimerId)
            where['disclaimer_id'] = { _eq: this.filter.disclaimerId };
        if (this.filter.fromDate)
            where['reference_date'] = { _gte: this.filter.fromDate };
        if (this.filter.toDate) {
            where['reference_date'] = { ...(where['reference_date'] ?? {}), _lte: this.filter.toDate };
        }
        return where;
    }
}
exports.MetricsFetcher = MetricsFetcher;
//# sourceMappingURL=metrics-fetcher.js.map