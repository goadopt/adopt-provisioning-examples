"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.VendorFetcher = void 0;
const base_fetcher_1 = require("./base-fetcher");
const vendor_1 = require("../graphql/queries/vendor");
const versions_fetcher_1 = require("./versions-fetcher");
class VendorFetcher extends base_fetcher_1.BaseFetcher {
    /**
     * Fetch vendors — automatically uses the current vendors_version so you only
     * see the active vendor assignments, not leftover rows from past updates.
     */
    async fetch(options = {}) {
        let version = this.filter.version;
        if (version === undefined && this.filter.disclaimerId) {
            const versions = await (0, versions_fetcher_1.getDisclaimerVersions)(this.client, this.filter.disclaimerId);
            version = versions?.vendors_version;
        }
        const where = this.buildVendorWhere(version);
        const result = await this.client.query(vendor_1.GET_DISCLAIMER_VENDORS, {
            where,
            limit: options.limit ?? 50,
            offset: options.offset ?? 0,
        });
        const totalCount = result.vendors_aggregate?.aggregate?.count ?? null;
        return {
            data: result.vendors,
            totalCount,
            hasMore: result.vendors.length === (options.limit ?? 50),
        };
    }
    where(filter) {
        return new VendorFetcher(this.client, { ...this.filter, ...filter });
    }
    buildVendorWhere(version) {
        const where = { deleted_at: { _is_null: true } };
        if (this.filter.disclaimerId)
            where['disclaimer_id'] = { _eq: this.filter.disclaimerId };
        if (this.filter.vendorId)
            where['vendor_id'] = { _eq: this.filter.vendorId };
        if (version !== undefined)
            where['version'] = { _eq: version };
        return where;
    }
}
exports.VendorFetcher = VendorFetcher;
//# sourceMappingURL=vendor-fetcher.js.map