"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BaseFetcher = void 0;
class BaseFetcher {
    constructor(client, filter) {
        this.client = client;
        this.filter = filter;
    }
    async *stream(options = {}) {
        const limit = options.limit ?? 50;
        let offset = 0;
        while (true) {
            const page = await this.fetch({ ...options, limit, offset });
            for (const item of page.data) {
                yield item;
            }
            if (!page.hasMore)
                break;
            offset += limit;
        }
    }
    buildWhere(filter) {
        return filter;
    }
}
exports.BaseFetcher = BaseFetcher;
//# sourceMappingURL=base-fetcher.js.map