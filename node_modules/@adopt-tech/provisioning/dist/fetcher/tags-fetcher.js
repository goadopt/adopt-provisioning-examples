"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TagsFetcher = void 0;
exports.isTagBlocked = isTagBlocked;
const base_fetcher_1 = require("./base-fetcher");
const tags_1 = require("../graphql/queries/tags");
const versions_fetcher_1 = require("./versions-fetcher");
/**
 * Returns true when a tag is effectively blocked — i.e. all its cookies
 * have blocked === true. A tag with no cookies is considered not blocked.
 */
function isTagBlocked(tag) {
    const cookies = tag.cookies ?? [];
    return cookies.length > 0 && cookies.every((c) => c.blocked);
}
class TagsFetcher extends base_fetcher_1.BaseFetcher {
    /**
     * Fetch tags — automatically uses the current scan version so you never
     * see tags from stale past scans. Override by setting filter.version explicitly.
     */
    async fetch(options = {}) {
        let version = this.filter.version;
        // Auto-resolve current version when disclaimerId is known and version is not pinned
        if (version === undefined && this.filter.disclaimerId) {
            const versions = await (0, versions_fetcher_1.getDisclaimerVersions)(this.client, this.filter.disclaimerId);
            version = versions?.tags_version;
        }
        const where = this.buildTagsWhere(version);
        const result = await this.client.query(tags_1.GET_DISCLAIMER_TAGS, {
            where,
            limit: options.limit ?? 100,
            offset: options.offset ?? 0,
            order_by: options.orderBy ?? [{ name: 'asc' }],
        });
        const totalCount = result.tags_aggregate?.aggregate?.count ?? null;
        return {
            data: result.tags,
            totalCount,
            hasMore: result.tags.length === (options.limit ?? 100),
        };
    }
    /**
     * Fetch tags split into blocked vs not-blocked based on actual cookie state.
     * Blocked = ALL cookies within the tag have blocked === true (not auto_block).
     * Version is resolved automatically.
     */
    async fetchWithBlockStatus(options = {}) {
        const page = await this.fetch(options);
        const blocked = page.data.filter(isTagBlocked);
        const notBlocked = page.data.filter((t) => !isTagBlocked(t));
        return { blocked, notBlocked, total: page.data.length };
    }
    /**
     * Alias for fetchWithBlockStatus() — contract-named variant that makes the
     * auto-versioning behaviour explicit in the method name.
     */
    fetchCurrentVersionWithBlockStatus(options = {}) {
        return this.fetchWithBlockStatus(options);
    }
    where(filter) {
        return new TagsFetcher(this.client, { ...this.filter, ...filter });
    }
    /** Filter by the auto-block FEATURE (not actual blocked state). */
    autoBlockEnabled(enabled) {
        return this.where({ autoBlockEnabled: enabled });
    }
    buildTagsWhere(version) {
        const where = { deleted_at: { _is_null: true } };
        if (this.filter.disclaimerId)
            where['disclaimer_id'] = { _eq: this.filter.disclaimerId };
        if (version !== undefined)
            where['version'] = { _eq: version };
        if (this.filter.tagCategory)
            where['tag_category'] = { _eq: this.filter.tagCategory };
        if (this.filter.autoBlockEnabled !== undefined)
            where['auto_block'] = { _eq: this.filter.autoBlockEnabled };
        return where;
    }
}
exports.TagsFetcher = TagsFetcher;
//# sourceMappingURL=tags-fetcher.js.map