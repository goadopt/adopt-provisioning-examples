"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CookieDocumentProvisioner = void 0;
const base_1 = require("./base");
const cookie_policy_1 = require("../graphql/mutations/cookie-policy");
const disclaimer_org_resolver_1 = require("../services/disclaimer-org-resolver");
const scan_manager_1 = require("../scan/scan-manager");
// Standard answers required by create_cookie_policy_document.
// Question IDs are stable across environments (seeded data).
// Only the date answer uses a dynamic value (current date).
const DEFAULT_COOKIE_POLICY_ANSWERS = () => [
    { questionId: '6b6bcedc-7da7-4630-87d4-3dd8ab2f4306', response: true },
    { questionId: '9c14b04e-ec10-4552-8f4b-83ade9b72b9a', response: '' },
    { questionId: '594bcb8e-3766-4a2d-bc8b-3296b62c7018', response: '' },
    { questionId: 'fe226b42-47a0-4ddb-9b5a-7ea2ca6bc390', response: true },
    { questionId: '7d2dac0f-38e0-497e-982c-e2ffeb3c6405', response: new Date().toISOString() },
];
class CookieDocumentProvisioner extends base_1.BaseProvisioner {
    getType() {
        return 'cookie-document';
    }
    getDependencyMeta() {
        return { parent: { type: 'disclaimer', fieldName: 'disclaimerId' } };
    }
    async executeCore(context) {
        const disclaimerId = context.parentId;
        if (!disclaimerId)
            throw new Error('CookieDocumentProvisioner requires a parent disclaimer ID');
        const organizationId = this.input.organizationId || await (0, disclaimer_org_resolver_1.resolveDisclaimerOrgId)(context.client, disclaimerId, context);
        if (this.input.autoGenerate) {
            return this.generateFromScanData(context, disclaimerId, organizationId);
        }
        const result = await context.client.addOrEditCookieDocument({ ...this.input, organizationId, disclaimerId });
        context.logger.info({ disclaimerId, language: this.input.language }, 'Cookie document saved');
        return result[0];
    }
    async generateFromScanData(context, disclaimerId, organizationId) {
        // Resolve scanId: explicit input wins; otherwise poll for the latest scan on this disclaimer.
        // With maxConcurrency:1 and ScanProvisioner running before this one, the scan record
        // is already in the DB and poll() will find it immediately.
        let scanId = this.input.scanId;
        if (!scanId) {
            const latestScan = await new scan_manager_1.ScanManager(context.client, context.logger).poll(disclaimerId);
            if (latestScan) {
                scanId = latestScan.id;
                context.logger.info({ disclaimerId, scanId }, 'Resolved scanId for cookie document generation');
            }
            else {
                context.logger.warn({ disclaimerId }, 'No scan found — cookie document will be created without AI generation');
            }
        }
        const { query, variables } = (0, cookie_policy_1.buildCookiePolicyMutation)({
            organizationId,
            language: this.input.language,
            disclaimerId,
            scanId,
            masterDocumentId: this.input.masterDocumentId,
            documentName: this.input.documentName ?? 'Cookie Policy',
            answers: DEFAULT_COOKIE_POLICY_ANSWERS(),
        });
        const genResult = await context.client.query(query, variables);
        const gen = genResult.create_cookie_policy_document;
        const ok = gen.success === true || gen.success === 'true';
        if (!ok)
            throw new Error(gen.message ?? gen.errorCode ?? 'create_cookie_policy_document failed');
        const rawData = gen.data;
        const docData = Array.isArray(rawData) ? rawData[0] : rawData;
        if (!docData?.documentMasterId)
            throw new Error('No documentMasterId returned');
        // Link the generated document to the disclaimer
        await context.client.query(cookie_policy_1.ADD_DOCUMENT_TO_DISCLAIMER, {
            masterDocumentId: docData.documentMasterId,
            disclaimerIds: [disclaimerId],
        });
        context.logger.info({ disclaimerId, documentMasterId: docData.documentMasterId, language: this.input.language }, 'Cookie document auto-generated from scan data');
        return docData;
    }
}
exports.CookieDocumentProvisioner = CookieDocumentProvisioner;
//# sourceMappingURL=cookie-document.js.map