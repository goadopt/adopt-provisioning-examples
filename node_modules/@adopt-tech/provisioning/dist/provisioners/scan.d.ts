import { BaseProvisioner, ExecutionContext } from './base';
import type { ScanTypeValue } from '../scan/scan-manager';
import type { ProvisioningNodeType, DependencyMeta } from '../types';
export interface ScanProvisionerInput {
    /** Defaults to ScanType.Basic */
    scanType?: ScanTypeValue;
    isAuthenticated?: boolean;
    keepLastConfigs?: boolean;
    /**
     * Mode controls how the provisioner behaves after triggering the scan:
     * - 'fire-and-forget' (default): triggers and returns immediately
     * - 'wait': blocks until the scan completes or times out
     */
    mode?: 'fire-and-forget' | 'wait';
    /** Only relevant when mode='wait'. Timeout in ms (default: 120_000) */
    timeoutMs?: number;
    /** Only relevant when mode='wait'. Poll interval in ms (default: 5_000) */
    pollIntervalMs?: number;
}
export declare class ScanProvisioner extends BaseProvisioner<ScanProvisionerInput> {
    getType(): ProvisioningNodeType;
    getDependencyMeta(): DependencyMeta;
    protected executeCore(context: ExecutionContext): Promise<unknown>;
}
//# sourceMappingURL=scan.d.ts.map