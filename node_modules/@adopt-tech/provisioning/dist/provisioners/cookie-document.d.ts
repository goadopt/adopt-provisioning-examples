import { BaseProvisioner, ExecutionContext } from './base';
import type { AddEditCookieDocumentInput, ProvisioningNodeType, DependencyMeta } from '../types';
export interface CookieDocumentInput extends Omit<AddEditCookieDocumentInput, 'disclaimerId'> {
    /**
     * When true, auto-generates the cookie policy document linked to the latest scan.
     * ScanProvisioner must run before this provisioner (maxConcurrency:1, scan first).
     * Uses standard default answers; the backend fills content via AI when the scan completes.
     */
    autoGenerate?: boolean;
    /** Pin to a specific scan ID when autoGenerate is true (optional — uses latest scan if omitted) */
    scanId?: string;
}
export declare class CookieDocumentProvisioner extends BaseProvisioner<CookieDocumentInput> {
    getType(): ProvisioningNodeType;
    getDependencyMeta(): DependencyMeta;
    protected executeCore(context: ExecutionContext): Promise<unknown>;
    private generateFromScanData;
}
//# sourceMappingURL=cookie-document.d.ts.map