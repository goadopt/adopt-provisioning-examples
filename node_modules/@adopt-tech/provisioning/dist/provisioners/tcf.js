"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TcfProvisioner = void 0;
const base_1 = require("./base");
class TcfProvisioner extends base_1.BaseProvisioner {
    getType() {
        return 'tcf';
    }
    getDependencyMeta() {
        return { parent: { type: 'disclaimer', fieldName: 'disclaimerId' } };
    }
    async executeCore(context) {
        const disclaimerId = context.parentId;
        if (!disclaimerId)
            throw new Error('TcfProvisioner requires a parent disclaimer ID');
        // 1. Enable TCF on the disclaimer options
        if (this.input.enableTcf !== false) {
            await context.client.setDisclaimerOptions(disclaimerId, { enableTCF: true });
            context.logger.info({ disclaimerId }, 'TCF enabled');
        }
        // 2. Assign TCF IAB vendors
        if (this.input.vendorIds.length > 0) {
            await context.client.editVendors({ disclaimerId, vendorIds: this.input.vendorIds });
            context.logger.info({ disclaimerId, count: this.input.vendorIds.length }, 'TCF vendors assigned');
        }
        this.state.ownership = 'updated';
    }
}
exports.TcfProvisioner = TcfProvisioner;
//# sourceMappingURL=tcf.js.map