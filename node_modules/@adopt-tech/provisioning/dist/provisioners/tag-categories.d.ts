import { BaseProvisioner, ExecutionContext } from './base';
import type { ProvisioningNodeType, DependencyMeta } from '../types';
export interface TagCategoryEntry {
    /** Tag ID from the `tags` table */
    id: string;
    /** Category ID from the `tags_categories` table */
    tagCategory: string;
    /** Whether this tag should be auto-blocked */
    autoBlock?: boolean;
}
export interface TagCategoriesInput {
    tags: TagCategoryEntry[];
    /** Uncheck all tags in the banner UI after publish (default: false) */
    uncheckTags?: boolean;
}
export declare class TagCategoriesProvisioner extends BaseProvisioner<TagCategoriesInput> {
    getType(): ProvisioningNodeType;
    getDependencyMeta(): DependencyMeta;
    protected executeCore(context: ExecutionContext): Promise<void>;
}
//# sourceMappingURL=tag-categories.d.ts.map