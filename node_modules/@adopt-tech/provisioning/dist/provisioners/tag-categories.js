"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TagCategoriesProvisioner = void 0;
const base_1 = require("./base");
const tag_categories_1 = require("../graphql/mutations/tag-categories");
class TagCategoriesProvisioner extends base_1.BaseProvisioner {
    getType() {
        return 'tag-categories';
    }
    getDependencyMeta() {
        return { parent: { type: 'disclaimer', fieldName: 'disclaimerId' } };
    }
    async executeCore(context) {
        const disclaimerId = context.parentId;
        if (!disclaimerId)
            throw new Error('TagCategoriesProvisioner requires a parent disclaimer ID');
        const result = await context.client.query(tag_categories_1.SAVE_PUBLISH_TAGS, {
            disclaimerId,
            tagsData: this.input.tags,
            uncheckTags: this.input.uncheckTags ?? false,
        });
        const r = result.save_publish_tags;
        const success = r.success === true || r.success === 'true';
        if (!success)
            throw new Error(r.message ?? r.errorCode ?? 'save_publish_tags failed');
        context.logger.info({ disclaimerId, count: this.input.tags.length }, 'Tag categories updated');
        this.state.ownership = 'updated';
    }
}
exports.TagCategoriesProvisioner = TagCategoriesProvisioner;
//# sourceMappingURL=tag-categories.js.map