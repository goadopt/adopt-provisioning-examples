import { Logger } from '../logging';
import { AdoptProvisioningClient } from '../client';
import { StateStorage } from '../state';
import { BaseProvisioner } from '../provisioners/base';
import type { OrganizationProvisioner } from '../provisioners/organization';
import type { ExecutionConfig, ProvisioningResult } from '../types';
export interface ProvisioningTree {
    organizations: OrganizationProvisioner[];
}
export interface OrchestratorOptions {
    client: AdoptProvisioningClient;
    stateId?: string;
    storage?: StateStorage;
    config?: ExecutionConfig;
    logger?: Logger;
}
export declare class ResumableProvisioningExecutor {
    private readonly client;
    private readonly stateId;
    private readonly storage;
    private readonly config;
    private readonly logger;
    constructor(options: OrchestratorOptions);
    execute(tree: ProvisioningTree): Promise<ProvisioningResult>;
    rollback(provisioners: BaseProvisioner[]): Promise<void>;
    private flattenTree;
    private restoreStateToProvisioners;
    getStateId(): string;
}
//# sourceMappingURL=orchestrator.d.ts.map