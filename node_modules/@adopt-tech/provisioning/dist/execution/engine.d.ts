import { Logger } from '../logging';
import { AdoptProvisioningClient } from '../client';
import type { BaseProvisioner } from '../provisioners/base';
import type { ProvisioningNodeState, ExecutionConfig } from '../types';
export interface EngineOptions {
    client: AdoptProvisioningClient;
    config?: ExecutionConfig;
    logger?: Logger;
}
export declare class ProvisioningExecutor {
    private readonly client;
    private readonly config;
    private readonly logger;
    private paused;
    constructor(options: EngineOptions);
    pause(): void;
    resume(): void;
    executeGroup(provisioners: BaseProvisioner[], parentId?: string, parentType?: BaseProvisioner['getType'] extends () => infer T ? T : never): Promise<ProvisioningNodeState[]>;
    private executeOne;
}
//# sourceMappingURL=engine.d.ts.map