"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ScanManager = exports.ScanType = exports.ScanState = void 0;
const logging_1 = require("../logging");
const errors_1 = require("../errors");
const scan_1 = require("../graphql/mutations/scan");
const scan_2 = require("../graphql/queries/scan");
const enums_1 = require("../enums");
Object.defineProperty(exports, "ScanState", { enumerable: true, get: function () { return enums_1.ScanState; } });
Object.defineProperty(exports, "ScanType", { enumerable: true, get: function () { return enums_1.ScanType; } });
const TERMINAL_STATES = [enums_1.ScanState.Completed, enums_1.ScanState.Error];
class ScanManager {
    constructor(client, logger) {
        this.client = client;
        this.logger = logger ?? (0, logging_1.createProvisioningLogger)();
    }
    /** Fire and forget — triggers the scan and returns the triggered scan immediately. */
    async trigger(disclaimerId, options = {}) {
        const result = await this.client.query(scan_1.SCAN_DISCLAIMER, {
            disclaimerId,
            scanType: options.scanType ?? enums_1.ScanType.Basic,
            isAuthenticated: options.isAuthenticated ?? false,
            keepLastConfigs: options.keepLastConfigs ?? false,
        });
        const scanId = result?.scan_disclaimer?.data?.['id'];
        this.logger.info({ disclaimerId, scanId }, 'Scan triggered');
        return { disclaimerId, scanId };
    }
    /** Poll once — fetches the latest scan status for a disclaimer. */
    async poll(disclaimerId) {
        const result = await this.client.query(scan_2.GET_SCANS, {
            where: { disclaimer_id: { _eq: disclaimerId } },
            limit: 1,
            order_by: [{ created_at: 'desc' }],
        });
        return result.scan[0] ?? null;
    }
    /**
     * Trigger and wait — fires the scan then polls until it completes or times out.
     * Acts like a synchronous call: resolves with the final scan record.
     */
    async triggerAndWait(disclaimerId, options = {}) {
        const timeoutMs = options.timeoutMs ?? 120000;
        const pollIntervalMs = options.pollIntervalMs ?? 5000;
        await this.trigger(disclaimerId, options);
        // Brief initial delay to let the scan be picked up by the queue
        await new Promise((resolve) => setTimeout(resolve, 2000));
        const deadline = Date.now() + timeoutMs;
        while (Date.now() < deadline) {
            const scan = await this.poll(disclaimerId);
            if (scan) {
                options.onProgress?.(scan.state);
                this.logger.info({ disclaimerId, state: scan.state }, 'Scan poll');
                if (TERMINAL_STATES.includes(scan.state)) {
                    if (scan.state === enums_1.ScanState.Error) {
                        throw new Error(`Scan failed with state "${scan.state}" for disclaimer ${disclaimerId}`);
                    }
                    return scan;
                }
            }
            await new Promise((resolve) => setTimeout(resolve, pollIntervalMs));
        }
        throw new errors_1.TimeoutError(`Scan for disclaimer ${disclaimerId} did not complete within ${timeoutMs}ms`);
    }
}
exports.ScanManager = ScanManager;
//# sourceMappingURL=scan-manager.js.map