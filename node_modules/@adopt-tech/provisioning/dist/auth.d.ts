import { AdoptEnvironmentName } from './environments';
export interface LoginResult {
    /** The Cognito idToken — contains Hasura JWT claims, use this as `token` in AdoptProvisioningClient */
    idToken: string;
    expiresAt: number;
}
/**
 * Authenticate via the Adopt backend API (POST /api/auth/login).
 * Returns the idToken (Cognito JWT with Hasura claims) that can be passed
 * directly to AdoptProvisioningClient as `token`.
 *
 * @param backendUrlOrEnv  'staging' | 'production' or a full backend base URL
 * @param email            User email
 * @param password         User password
 */
export declare function login(backendUrlOrEnv: string | AdoptEnvironmentName, email: string, password: string): Promise<LoginResult>;
//# sourceMappingURL=auth.d.ts.map